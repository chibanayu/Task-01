<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta charset="UTF-8">
        <title>User Edit</title>
    </head>
    <body>
        <!--formの％は全て消した状態で行うか？-->
        <h1>更新フォーム</h1>
        <form action="/update/<%= user[0].id %>" method="POST">
            <p>id <input type="text" name="id" value="<%= user[0].id %>"></p>
            <p>名前 <input type="text" name="name" value="<%= user[0].name %>"></p>
            <p>ふりがな <input type="text" name="kana_name" value="<%= user[0].kana_name %>"></p>
            <p>性別 <input type="text" name="gender" value="<%= user[0].gender %>"></p>
            <p>email <input type="text" name="email" value="<%= user[0].email %>"></p>
            <p>電話番号 <input type="text" name="phone" value="<%= user[0].phone %>"></p>
            <p>住所 <input type="text" name="address" value="<%= user[0].address %>"></p>
            <p>都道府県 <input type="text" name="workplace" value="<%= user[0].workplace %>"></p>
            <p>世帯 <input type="text" name="household" value="<%= user[0].household %>"></p>
            <button type="submit">更新</button>
        </form>
        <style>
            input {
               position: absolute;
               left: 90px;
            }

            button {
                position: absolute;
               left: 190px;
            }
        </style>
    </body>
</html>
